{
	"manifest_version": 2,
	"name": "Zotero Git Sync -- Fixes Test",
	"version": "0.0.1",
	"description": "DEVELOPMENT ONLY. Checks the 0.1.4 fixes (prune preference, keep both for a multi-file attachment, repository paths that leave the attachment folder) against a throwaway repository, writes a report, and quits. Never install in a real profile.",
	"applications": {
		"zotero": {
			"id": "zgit-fixes-test@situkangsayur.github.io",
			"update_url": "https://example.invalid/none.json",
			"strict_min_version": "6.999",
			"strict_max_version": "10.*"
		}
	}
}
